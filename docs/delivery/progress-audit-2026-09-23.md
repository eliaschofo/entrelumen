# Scope checkpoint — 2026-09-23

Owner: root. The previous turn made verified progress: commit `2c9d9608f85c4386b9a14e3deb637962e5f39397` fixes the observed backpack tooltip collision, includes EN/ES runtime evidence and passed CI run `35847390147`. It does not close campaign, scale or performance acceptance. The goal and its existing accounting remain active.

At the start of this checkpoint, the catalog had 151 client / 116 server dependencies plus the companion; the campaign prototype has 171 quests. The approved extra-large outcome and provisional 600-project / 5,000–5,500-quest planning scope are unchanged. The natural first-hour playtest is still missing. Its gate prevents mass quest authoring, not independent work on required performance or functional systems.

The exact next target is `performance_target_hardware` in `docs/verification/acceptance.json`. The existing real capture is stationary in a creative Act VI fixture and deliberately remains incomplete. It verifies the collector, not a working early base or moving route. Read-only inspection of `BenchmarkClient` confirms that riding a minecart does not invalidate its frame/tick predicates.

Bounded action: create a separate deterministic QA early-base workload with operating furnaces, hoppers, storage, crops and livestock, then run a closed minecart route on the target machine with the existing collector. Verify actual processing and route movement before capture; preserve the prepared save hash, setup recipe, native images and all consecutive raw samples. Record at least 300 seconds of frames and ticks at the agreed preset, with warmup separate. This establishes a prototype workload baseline, not the final 600-mod pack, industrial bases, exploration, six-player load, collector overhead or two-hour stability. Those requirements remain open whatever the numerical result.

Continuation outcome: progress. The generator and six structural checks exist; the isolated normal world loaded the apparatus, the mounted cart moved and processors lit. Offline inspection after normal client shutdown proves eight output barrels each contain five iron ingots and two each contain ten cooked beef, with all sixteen tagged animals retained. A private hash-verified snapshot preserves the prepared world. No measurement was started. The user now explicitly requests continuing **without Computer Use**; retain the pending capture and advance work that can be integrated and verified without desktop control. Evidence: `docs/verification/early-base-fixture.md` and its runtime JSON. The performance acceptance remains pending.

The fixture was integrated in `871baf6063e1547f32e42bd8abded98ca516d512`; CI `35856065920` passed. The next concurrent delivery expands the RFTools/XNet technical family with pinned, original integration and validates Botany Pots/Tiers plus actual AE2 autocrafting on the owned dedicated server. Separate owners handle catalog/recipes, resource-farm QA and AE2 QA; root owns installation, runtime and integration. An independent recipe review found the native pot-tier chain intact and rejected speculative permission tokens that could create project/component cycles. No new campaign gates, buffs or mass quests are introduced merely to occupy the desktop pause.

Concurrent delivery outcome: the six RFTools/XNet dependency files and 11 native-recipe adjustments are installed in both stopped clients and the owned server, preserving all prior 151 catalog entries. The normal dedicated server started and saved with no ERROR lines. Five native farm/RFTools cases passed; the AE2 case exposed a QA requester without a machine node, was corrected without changing recipes, and then completed two crafts with exactly two contracts and eight returned bowls. Six new cases therefore have bounded PASS evidence across the recorded runs. The original production companion was restored and all owned server processes stopped. Current dependency counts are 157 client / 122 server; quests remain 171. Client launch, performance, natural first-hour survival, final-scale campaign and publication remain open. See `docs/verification/rftools-resource-runtime.json`.

Next continuation classification: progress. The previous RFTools delivery was committed as `c4c9c74ac966d7a2955eeaf91e1e76aaee8b3d68` and CI `35860412880` passed. Separate workers then owned magic curation, food/provision integration and native team QA; root integrated the catalog, installation and runtime. Thirteen new projects now provide 170 client / 135 server dependencies. Two added ration routes, nine suppressed duplicate tree placements with live market access, team lifecycle and AE2 autocrafting have four directed PASS results. The initial server exposed a third-party client mixin in AE2 card 1.6.0; official 1.5.0 avoids it, and the final normal server started/saved without ERROR lines. All previous 157 catalog entries remain identical; the production companion is unchanged and every owned server stopped. See `docs/verification/magic-cooking-teams-runtime.json`. The clients remain unlaunched under the no-Computer-Use instruction. This does not validate magic machine workloads, biome density, survival pacing, client performance or release-scale acceptance. Quests remain 171 and the natural first-hour gate still prevents mass authoring. No goal accounting or completion state was changed.


Continuation outcome: product progress. The engineering module gained a practical material repair service at a complete physical Ark, preserving component data and unrestricted gifted use. The current engineering quest and hints explain it in EN/ES. Two full-pack repair cases passed; native Ars/Create and Ars/Occultism flows passed directly. Ars/AE2 initially failed because the test consulted cached global stored power before AE2 refreshed its index; a bounded native-index wait with physical cell observations corrected the test, and the real Source-to-ME flow then passed. No magic recipe or power balance was changed to pass QA. The final normal dedicated server started and saved cleanly; the normal companion and two quest translations are installed in all three profiles, with unrelated settings preserved and owned processes stopped. Counts remain 170/135 dependencies and 171 quests. The other full module benefits, natural first-hour playtest, final-scale campaign, client/performance review, public release and weekly-review acceptance remain open. Evidence: docs/verification/ark-workshop-magic-runtime.json. Goal accounting is unchanged.

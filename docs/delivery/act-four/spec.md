# Act IV: recorded expeditions and the voices of the Atlas

Continue the full authorized E2E goal with four integrated prototypes, three real dimension observations, the preserved atlas_voices closure and one original bilingual teaching chapter. This does not shrink extra-large mod/quest, art, survival, performance or publication acceptance.

Authoritative project IDs and prerequisites: spectral_archive consumes one spectral_lens after exchange_route; horizon_survey consumes one horizon_chart after exchange_route+aether_arrival+twilight_arrival; pollinator_treaty consumes one ecosystem_capsule after exchange_route+bumblezone_arrival; sealed_memory consumes one containment_seal after exchange_route+spectral_archive; atlas_voices consumes3paper+1copper after all four new projects. Each prototype is an entrelumen item. Rewards empty. Historical completed IDs remain. Static recipes and unrestricted gifts/use stay unchanged; gifted samples do not fabricate recorded journeys.

Expeditions.IDS are aether_arrival, twilight_arrival, bumblezone_arrival. Exact dimension resource IDs must be inspected in pinned mod JARs before implementation. Expeditions records server-observed destination on successful dimension change or login/respawn in that destination, never from client claims, inventory or quest clicks. Record on any act so early exploration counts, without advancing acts or completing delivery projects. Existing Campaign.completed persistence/copies carry these observation IDs: no schema or wire change is needed. Archived campaigns reject recording; repeated observation does not dirty or reward. Current team only, no merge or retroactive sharing on join. A member's successful observation serves the team campaign, not every player's private snapshot.

Projects permits only the known observation IDs as external prerequisites, forbids defining a deliverable with those reserved IDs, keeps normal project validation/DAG constraints and skips observations when traversing project nodes. Existing Atlas prerequisite rows and FTB campaign mirrors show the same observer IDs with EN/ES labels. Five deliverable milestones plus three observation milestones form the new chapter's campaign graph; optional tutorials never block a delivery. Stable prior 75 questIDs/text/layout are preserved.

Ownership: root integrates Projects.java, Entrelumen.java, project JSON, languages, RuntimeGameTests and evidence. A bounded observer writer owns only Expeditions.java and ExpeditionsTest.java. A bounded quest writer owns act_four.json, generate_quests.py, test_generate_quests.py, generated FTB files/campaign_task_ids.json and content/INTEGRATION.md. Acquisition auditor is read-only except a work-directory evidence note. No redelegation; root runs all Java builds after writers finish.

Acceptance: validate four native recipe chains and actual loaded ingredients/crafting; event observations survive persistence and team changes; gifts cannot bypass visits; five real server deliveries consume exactly once, respect missing prerequisites and advance IV to V; old quests remain stable and new graph/EN/ES load in FTB. Source tests and synthetic supplies are not survival or portal playtests. Computer Use remains paused after Escape until explicit availability; do not modify the running client. Full-pack server is owned/stopped and can be updated/tested with backups.

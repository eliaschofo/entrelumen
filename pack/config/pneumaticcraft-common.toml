# ENTRELUMEN: jetpacks pay 2.5 times their native cost per tick (tools/generate_jetpack_balance.py).
# NeoForge fills in every other key with its default on the first start.

[pneumatic_armor]
	# mod default 12
	jet_boots_air_usage = 30
